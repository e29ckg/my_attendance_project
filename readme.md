เยี่ยมเลยครับ การย้ายค่า Config ไปลง `.env` ถือว่าเป็นการจัดการระบบที่ถูกต้องครับ

เพื่อให้คู่มือการใช้งาน (README) ทันสมัยและครอบคลุมฟีเจอร์ใหม่ทั้งหมด (ระบบฉุกเฉิน, แสดงชื่อไทย, Timestamp, และ Config แบบใหม่) ผมขอสรุปสิ่งที่ต้องอัปเดตลงใน **README.md** ให้ดังนี้ครับ

คุณสามารถก๊อปปี้เนื้อหานี้ไปทับไฟล์ **`README.md`** เดิมได้เลยครับ

---

# 📷 AI Face Recognition Time Attendance System

**ระบบบันทึกเวลาทำงานด้วยใบหน้า (Face Recognition)** พร้อมระบบจัดการผ่านเว็บ, แจ้งเตือน Telegram และโหมดฉุกเฉินสำหรับกรอกรหัส

---

## ✨ ฟีเจอร์หลัก (Features)

### 🖥️ ฝั่ง Client (เครื่องสแกน / Kiosk)

* **Real-time Detection:** ตรวจจับใบหน้าและส่งไปประมวลผลที่ Server
* **Thai Name Display:** แสดงชื่อพนักงานภาษาไทยบนกรอบตรวจจับใบหน้า
* **Smart Timestamp:** ฝังวันที่และเวลาลงในภาพหลักฐาน (Watermark) ก่อนส่งบันทึก
* **Voice Greeting:** พูดทักทายชื่อพนักงานเมื่อสแกนผ่าน (TTS ภาษาไทย)
* **Emergency Mode (Manual Input):** ระบบฉุกเฉินสำหรับกรอกรหัสพนักงาน กรณีสแกนไม่ติด หรือใส่หน้ากาก
* **Auto Config:** ตั้งค่า IP Server และกล้องผ่านไฟล์ `.env` ไม่ต้องแก้โค้ด

### ⚙️ ฝั่ง Server (Brain & Database)

* **DeepFace AI:** ใช้โมเดล `Facenet512` แม่นยำสูง
* **System Monitor:** หน้าจอ Dashboard ตรวจสอบสถานะ CPU, RAM, Disk และการเชื่อมต่อ
* **Auto Cleanup:** ระบบลบรูปภาพหลักฐานเก่าอัตโนมัติ (ตั้งค่าจำนวนวันได้)
* **Telegram Notification:** แจ้งเตือนพร้อมส่งรูปถ่ายเข้า Line/Telegram ทันที

---

## 🛠️ การติดตั้ง (Installation)

### 1. เตรียมความพร้อม

* Python 3.10 ขึ้นไป
* Webcam
* ไฟล์ `haarcascade_frontalface_default.xml` (สำหรับ OpenCV)

### 2. สร้าง Virtual Environment (แนะนำ)

```bash
python -m venv venv
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

```

### 3. ติดตั้ง Library

ตรวจสอบให้แน่ใจว่าไฟล์ `requirements.txt` มีรายชื่อ library ครบถ้วน (รวม `Pillow`, `python-dotenv`) แล้วรันคำสั่ง:

```bash
pip install -r requirements.txt

```

---

## ⚙️ การตั้งค่าระบบ (.env)

ระบบใช้ไฟล์ **`.env`** เพียงไฟล์เดียวในการตั้งค่าทั้ง Server และ Client สร้างไฟล์ชื่อ `.env` ที่โฟลเดอร์หลัก และใส่ข้อมูลดังนี้:

```env
# ==============================
# 🔧 SERVER CONFIG (เครื่องแม่)
# ==============================
HOST=0.0.0.0
PORT=9876

# ความแม่นยำ AI (0.20 - 0.40) ยิ่งน้อยยิ่งเข้มงวด
THRESHOLD=0.30

# การเก็บรักษาข้อมูล
DB_FILE=attendance.db
KEEP_IMAGE_DAYS=15

# แจ้งเตือน Telegram
ENABLE_TELEGRAM=True
TELEGRAM_TOKEN=ใส่_Token_จาก_BotFather
TELEGRAM_CHAT_ID=ใส่_Chat_ID_ของกลุ่ม

# ==============================
# 📷 CLIENT CONFIG (เครื่องสแกน)
# ==============================
# IP ของเครื่อง Server (ถ้าเครื่องเดียวกันใช้ localhost)
SERVER_URL=http://localhost:9876

# เลขกล้อง (0 = กล้องหลัก/Notebook, 1 = กล้อง USB)
CAMERA_INDEX=0

# ระยะเวลาเช็คการเชื่อมต่อ Server (วินาที)
CHECK_INTERVAL=5

```

---

## 🚀 วิธีเริ่มใช้งาน (Usage)

### 1. รัน Server (Processing Unit)

```bash
# Activate venv ก่อน
python server_api.py

```

* หรือใช้ไฟล์ `run_server.bat` (บน Windows)
* เข้าจัดการระบบผ่านเว็บ: `http://localhost:9876`

### 2. รัน Client (Kiosk Unit)

```bash
# Activate venv ก่อน
python client_kiosk.py

```

* หรือใช้ไฟล์ `run_client.bat` (บน Windows)
* **การใช้งาน:**
* มองกล้องเพื่อลงเวลา
* หากสแกนไม่ติด ให้กดปุ่ม **"⌨️ ระบบฉุกเฉิน"** เพื่อกรอกรหัสพนักงานและถ่ายรูป



---

## 📂 โครงสร้างไฟล์ (File Structure)

```text
Project_Folder/
 ├── .env                     # [สำคัญ] ไฟล์ตั้งค่าระบบ
 ├── requirements.txt         # รายชื่อ Library
 ├── server_api.py            # โค้ดฝั่ง Server
 ├── client_kiosk.py          # โค้ดฝั่ง Client
 ├── haarcascade...xml        # ไฟล์ OpenCV Face Detect
 │
 ├── index.html               # หน้าเมนูหลัก
 ├── admin.html               # หน้าจัดการพนักงาน
 ├── report_daily.html        # หน้ารายงานรายวัน
 ├── monitor.html             # หน้าตรวจสอบระบบ (Monitor)
 │
 ├── run_server.bat           # สคริปต์เปิด Server
 └── run_client.bat           # สคริปต์เปิด Client

```

---

## ❓ Troubleshooting (การแก้ปัญหา)

* **ภาษาไทยเป็น ???? หรือสี่เหลี่ยม:**
* ระบบจะพยายามใช้ฟอนต์ `tahoma.ttf` ของ Windows
* หากใช้บน Linux/Mac อาจต้องติดตั้งฟอนต์ไทย หรือแก้โค้ดให้ชี้ไปยัง path ฟอนต์ที่ถูกต้อง (.ttf)


* **Client ฟ้อง Offline:**
* ตรวจสอบ IP Address ในไฟล์ `.env` ตรง `SERVER_URL`
* ตรวจสอบ Firewall เครื่อง Server ว่าเปิดพอร์ต 9876 หรือไม่


* **TensorFlow Error (DLL load failed):**
* ติดตั้ง **Microsoft Visual C++ Redistributable (x64)**
* ตรวจสอบว่า CPU รองรับ AVX Instruction Set หรือไม่



---

### 📝 เพิ่มเติม: อัปเดต `requirements.txt`

อย่าลืมเพิ่ม `Pillow` (สำหรับการวาดภาษาไทย) ลงในไฟล์ requirements ของคุณด้วยนะครับ

```text
fastapi
uvicorn
python-multipart
deepface
opencv-python
numpy
requests
python-dotenv
psutil
shutil
pillow    <-- เพิ่มตัวนี้
PyQt6
gTTS
pygame

```