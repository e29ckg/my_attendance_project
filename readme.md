นี่คือไฟล์ **README.md** ฉบับสมบูรณ์ที่อัปเดตล่าสุดตามฟีเจอร์ทั้งหมดที่เราทำกันมาครับ (รองรับ .env, Telegram, จัดการตำแหน่ง, และพิมพ์รายงานแบบราชการ)

คุณสามารถก๊อปปี้ข้อความด้านล่างไปสร้างไฟล์ชื่อ `README.md` ไว้ในโฟลเดอร์โปรเจกต์ได้เลยครับ

---

# 📷 AI Face Recognition Time Attendance System

**ระบบบันทึกเวลาทำงานด้วยใบหน้า (Client-Server Architecture)**

ระบบลงเวลาทำงานอัตโนมัติด้วย AI (DeepFace) แยกการทำงานระหว่าง **เครื่องสแกน (Client)** และ **เครื่องประมวลผล (Server)** พร้อมระบบจัดการผ่านเว็บ (Web Admin), ออกรายงานราชการ (A4), และแจ้งเตือนผ่าน Telegram

---

## ✨ ฟีเจอร์หลัก (Features)

### 🖥️ ฝั่ง Client (เครื่องสแกน / Kiosk)

* **Real-time Detection:** ตรวจจับใบหน้าและส่งไปประมวลผลที่ Server
* **Smart Heartbeat:** เช็คสถานะ Server ตลอดเวลา (🟢 กรอบเขียว = พร้อม / 🔴 กรอบแดง = Server ดับ)
* **Voice Greeting:** พูดชื่อพนักงานเมื่อสแกนผ่าน (TTS ภาษาไทย) และจำได้ว่าทักไปแล้ว (ไม่พูดซ้ำ)
* **Display:** แสดงวันเวลาแบบไทย (พ.ศ.) และรูปกล้อง Real-time

### ⚙️ ฝั่ง Server (Brain & Database)

* **DeepFace AI:** ใช้โมเดล `Facenet512` ความแม่นยำสูง
* **RESTful API:** เชื่อมต่อด้วย FastAPI รองรับ Client หลายตัว
* **Auto Sort Report:** รายงานเรียงตามเวลาเข้างาน (มาก่อนอยู่บน / มาทีหลังอยู่ล่าง)
* **Telegram Notification:** ส่งรูปและเวลาเข้าแชท Telegram ทันทีที่สแกนติด
* **Environment Config:** ตั้งค่าระบบผ่านไฟล์ `.env` ปลอดภัยและง่าย

### 🌐 ฝั่ง Web Management (Admin & Report)

* **Dashboard:** รวมเมนูเข้าถึงง่าย (`/`)
* **Employee Management:** เพิ่ม/ลบ/แก้ไข ข้อมูลพนักงาน รูปภาพ และ **จัดการตำแหน่ง (Roles)**
* **Daily Report:** ดูเวลา เข้า-ออก (First-in / Last-out) รายวัน
* **Official Print:** พิมพ์แบบฟอร์มลงเวลาราชการ (A4) พร้อมสรุปยอด มา/ลา/สาย อัตโนมัติ

---

## 🛠️ การติดตั้ง (Installation)

### 1. สิ่งที่ต้องมี (Prerequisites)

* Python 3.8 - 3.10 (แนะนำ 3.10)
* Webcam (สำหรับเครื่อง Client)

### 2. ติดตั้ง Library

รันคำสั่งนี้ทั้งที่เครื่อง **Server** และ **Client**:

```bash
pip install fastapi uvicorn python-multipart deepface opencv-python numpy requests PyQt6 gTTS pygame python-dotenv

```

---

## ⚙️ การตั้งค่า (Configuration)

### 1. ตั้งค่า Server (`.env`)

สร้างไฟล์ชื่อ `.env` ไว้ที่เดียวกับ `server_api.py` และใส่ข้อมูลดังนี้:

```env
# --- SERVER CONFIG ---
PORT=9876
HOST=0.0.0.0

# --- AI CONFIG ---
# ค่าความเหมือน (0.25 - 0.40) ยิ่งน้อยยิ่งเข้มงวด
THRESHOLD=0.30

# --- DATABASE ---
DB_FILE=attendance.db

# --- TELEGRAM NOTIFICATION ---
ENABLE_TELEGRAM=True
TELEGRAM_TOKEN=ใส่_Token_จาก_BotFather
TELEGRAM_CHAT_ID=ใส่_Chat_ID_ของกลุ่ม

```

### 2. ตั้งค่า Client (`client_kiosk.py`)

เปิดไฟล์ `client_kiosk.py` และแก้ไข IP ของเครื่อง Server:

```python
# แก้ localhost เป็น IP ของเครื่อง Server (เช่น 192.168.1.50)
SERVER_URL = "http://localhost:9876" 

```

---

## 🚀 วิธีเริ่มใช้งาน (Usage)

### ขั้นตอนที่ 1: รัน Server (เครื่องแม่)

```bash
python server_api.py

```

*Server จะเริ่มทำงานที่ `http://0.0.0.0:9876*`

### ขั้นตอนที่ 2: รัน Client (เครื่องสแกน)

```bash
python client_kiosk.py

```

*หน้าจอ Kiosk จะเปิดขึ้นมาพร้อมกล้อง*

### ขั้นตอนที่ 3: ใช้งานหน้าเว็บ

เปิด Browser แล้วเข้าผ่าน URL:

* **เมนูหลัก:** `http://localhost:9876`
* **จัดการพนักงาน:** `http://localhost:9876/admin`
* **พิมพ์รายงาน:** `http://localhost:9876/print`

---

## 📂 โครงสร้างไฟล์ (File Structure)

```text
📦 Project_Folder
 ┣ 📜 .env                # [Server] ไฟล์ตั้งค่าระบบ (ห้ามลืมสร้าง!)
 ┣ 📜 server_api.py       # [Server] โค้ดหลัก Server
 ┣ 📜 client_kiosk.py     # [Client] โค้ดจอเครื่องสแกน
 ┣ 📜 index.html          # [Web] หน้าเมนูรวม
 ┣ 📜 admin.html          # [Web] หน้าจัดการพนักงาน
 ┣ 📜 report_daily.html   # [Web] หน้ารายงานประจำวัน
 ┣ 📜 report_print.html   # [Web] หน้าพิมพ์แบบฟอร์ม A4
 ┣ 📜 haarcascade_frontalface_default.xml # ไฟล์ OpenCV Detect หน้า
 ┣ 📂 images/             # เก็บรูปโปรไฟล์ (ระบบสร้างเอง)
 ┣ 📂 attendance_images/  # เก็บรูปหลักฐานการสแกน (ระบบสร้างเอง)
 ┗ 📜 attendance.db       # ฐานข้อมูล SQLite (ระบบสร้างเอง)

```

---

## ❓ การแก้ไขปัญหาเบื้องต้น (Troubleshooting)

* **Client ขึ้น Offline 🔴:**
* เช็ค IP Address ใน `client_kiosk.py` ว่าตรงกับเครื่อง Server ไหม
* เช็ค Firewall ของเครื่อง Server ว่าบล็อก Port 9876 หรือไม่


* **บันทึกข้อมูลไม่ได้ / ไม่แจ้งเตือน:**
* ระบบมี Cooldown 1 นาที (สแกนซ้ำภายใน 1 นาทีจะไม่บันทึกเพิ่ม)
* เช็คไฟล์ `.env` ว่าใส่ Token Telegram ถูกต้องไหม


* **หน้าเว็บโหลดรูปไม่ขึ้น:**
* ตรวจสอบว่าใน `server_api.py` มีคำสั่ง `app.mount` ครบทั้ง `/images` และ `/attendance_images` แล้ว



---

Made with ❤️ using Python & DeepFace