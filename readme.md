
# 📷 AI Face Recognition Time Attendance System

**ระบบบันทึกเวลาทำงานด้วยใบหน้า (Client-Server Architecture)**

ระบบลงเวลาทำงานอัตโนมัติด้วย AI (DeepFace) แยกการทำงานระหว่าง **เครื่องสแกน (Client)** และ **เครื่องประมวลผล (Server)** พร้อมระบบจัดการผ่านเว็บ, รายงานผลแบบ Real-time, พิมพ์แบบฟอร์มราชการ และแจ้งเตือนผ่าน Telegram

---

## ✨ ฟีเจอร์หลัก (Features)

### 🖥️ ฝั่ง Client (เครื่องสแกน / Kiosk)

* **Real-time Detection:** ตรวจจับใบหน้าและส่งไปประมวลผลที่ Server
* **Voice Greeting:** พูดชื่อพนักงานเมื่อสแกนผ่าน (TTS ภาษาไทย)
* **Smart Interface:** แสดงผลวันเวลาแบบไทย และสถานะการเชื่อมต่อ Server (🟢/🔴)

### ⚙️ ฝั่ง Server (Brain & Database)

* **DeepFace AI:** ใช้โมเดล `Facenet512` แม่นยำสูง
* **System Monitor:** หน้าจอตรวจสอบสถานะ CPU, RAM, Disk และการเชื่อมต่อต่างๆ
* **Auto Cleanup:** ระบบลบรูปภาพเก่าอัตโนมัติ (ตั้งค่าวันได้)
* **Telegram Notification:** แจ้งเตือนพร้อมรูปถ่ายเข้า Line/Telegram ทันที

### 🌐 ฝั่ง Web Management

* **Admin Dashboard:** จัดการพนักงาน (เพิ่ม/ลบ/แก้ไข) และจัดการตำแหน่ง (Roles)
* **Daily Report:** รายงานเวลา เข้า-ออก เรียงตามเวลามาทำงาน
* **View Evidence:** กดดูรูปถ่ายตอนสแกนได้จากหน้ารายงาน
* **Print A4:** พิมพ์แบบฟอร์มลงเวลาราชการ พร้อมสรุปยอด มา/สาย/ขาด

---

## 🛠️ การติดตั้ง (Installation)

### 1. เตรียมความพร้อม (Prerequisites)

* Python 3.10+
* Webcam (สำหรับเครื่อง Client)
* ไฟล์ `haarcascade_frontalface_default.xml` (สำหรับ OpenCV)

### 2. สร้าง Virtual Environment (venv)

เพื่อจำลองสภาพแวดล้อมและป้องกัน Library ตีกัน ให้ทำตามขั้นตอนนี้:

1. เปิด **Command Prompt (cmd)** หรือ Terminal ในโฟลเดอร์โปรเจกต์
2. พิมพ์คำสั่งสร้าง `venv`:
```bash
python -m venv venv

```


3. **เปิดใช้งาน (Activate) venv:**
* **Windows:**
```bash
venv\Scripts\activate

```


* *(สังเกตหน้าบรรทัดจะมีคำว่า `(venv)` ปรากฏขึ้นมา แสดงว่าสำเร็จ)*



### 3. ติดตั้ง Library

เมื่ออยู่ในสถานะ `(venv)` แล้ว ให้รันคำสั่งนี้เพื่อติดตั้งทุกอย่าง:

```bash
pip install -r requirements.txt

```

---

## ⚙️ การตั้งค่าระบบ (Configuration)

### 1. ตั้งค่า Server (`.env`)

สร้างไฟล์ชื่อ `.env` ไว้ที่โฟลเดอร์หลัก และใส่ข้อมูลดังนี้:

```env
# --- SERVER CONFIG ---
PORT=9876
HOST=0.0.0.0

# --- AI CONFIG (0.25 - 0.40 ยิ่งน้อยยิ่งเข้มงวด) ---
THRESHOLD=0.30

# --- DATABASE & STORAGE ---
DB_FILE=attendance.db
KEEP_IMAGE_DAYS=15

# --- TELEGRAM NOTIFICATION ---
ENABLE_TELEGRAM=True
TELEGRAM_TOKEN=ใส่_Token_จาก_BotFather
TELEGRAM_CHAT_ID=ใส่_Chat_ID_ของกลุ่ม

```

### 2. ตั้งค่า Client (`client_kiosk.py`)

เปิดไฟล์ `client_kiosk.py` และแก้ไข IP ของเครื่อง Server:

```python
# หากรันเครื่องเดียวกันใช้ localhost
# หากแยกเครื่อง ให้ใส่ IP ของเครื่อง Server (เช่น 192.168.1.50)
SERVER_URL = "http://localhost:9876" 

```

---

## 🚀 วิธีเริ่มใช้งาน (Usage)

คุณสามารถรันผ่านไฟล์ `.bat` ได้เลย (ระบบจะเรียก venv ให้อัตโนมัติถ้าตั้งค่าไว้ใน bat) หรือรันผ่าน cmd ดังนี้:

### 1. รัน Server (เครื่องแม่)

```bash
# ต้อง Activate venv ก่อนเสมอ
venv\Scripts\activate
python server_api.py

```

*หรือดับเบิ้ลคลิกไฟล์ `run_server.bat*`

### 2. รัน Client (เครื่องสแกน)

```bash
# ต้อง Activate venv ก่อนเสมอ
venv\Scripts\activate
python client_kiosk.py

```

*หรือดับเบิ้ลคลิกไฟล์ `run_client.bat*`

### 3. เข้าใช้งานหน้าเว็บ

เปิด Browser ไปที่:

* **หน้าหลัก:** `http://localhost:9876`
* **จัดการพนักงาน:** `http://localhost:9876/admin`
* **รายงาน:** `http://localhost:9876/report`
* **Monitor:** `http://localhost:9876/monitor`

---

## 📂 โครงสร้างไฟล์ (File Structure)

```text
Project_Folder/
 ├── venv/                    # [Auto] โฟลเดอร์ Virtual Environment
 ├── images/                  # [Auto] รูปโปรไฟล์พนักงาน
 ├── attendance_images/       # [Auto] รูปหลักฐานการสแกน
 ├── attendance.db            # [Auto] ฐานข้อมูล
 │
 ├── server_api.py            # โค้ดหลัก Server
 ├── client_kiosk.py          # โค้ดจอเครื่องสแกน
 ├── .env                     # ไฟล์ตั้งค่า (สร้างเอง)
 ├── requirements.txt         # รายชื่อ Library
 ├── haarcascade...xml        # ไฟล์ OpenCV
 │
 ├── index.html               # หน้าเมนู
 ├── admin.html               # หน้า Admin
 ├── report_daily.html        # หน้ารายงาน
 ├── report_print.html        # หน้าพิมพ์ A4
 ├── monitor.html             # หน้าตรวจสอบระบบ
 │
 ├── run_server.bat           # ตัวรัน Server (Windows)
 └── run_client.bat           # ตัวรัน Client (Windows)

```

---

## ❓ Troubleshooting

* **รันแล้วขึ้น `Module not found`:**
* ตรวจสอบว่าได้ Activate venv แล้วหรือยัง (ต้องมี `(venv)` หน้าบรรทัด cmd)
* ลองรัน `pip install -r requirements.txt` อีกครั้ง


* **Client ฟ้อง Offline:**
* เช็ค IP Address ใน `client_kiosk.py`
* เช็ค Firewall เครื่อง Server


* **Telegram ไม่แจ้งเตือน:**
* เช็ค Token และ Chat ID ในไฟล์ `.env` (ต้อง Restart Server หลังแก้ไฟล์ .env ทุกครั้ง)



---

Made with ❤️ using Python, FastAPI & DeepFace