# 📸 AI Face Attendance System (ระบบลงเวลาเข้างานด้วยใบหน้า)

ระบบบันทึกเวลาทำงานอัจฉริยะด้วยเทคโนโลยีจดจำใบหน้า (Face Recognition) พัฒนาด้วย **Python (FastAPI)** และ **DeepFace** มาพร้อมระบบป้องกันการปลอมแปลง (Liveness Detection), แจ้งเตือนผ่าน Telegram, และ Dashboard บริหารจัดการครบวงจร

![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)
![FastAPI](https://img.shields.io/badge/FastAPI-Latest-green.svg)
![OpenCV](https://img.shields.io/badge/OpenCV-Computer%20Vision-red.svg)
![MySQL](https://img.shields.io/badge/Database-MySQL-orange.svg)

## ✨ ฟีเจอร์หลัก (Key Features)

* **🚀 Real-time Recognition:** ตรวจจับและระบุตัวตนด้วยความแม่นยำสูง (Model: Facenet512)
* **👁️ Liveness Detection:** ระบบป้องกันการเอารูปถ่ายมาสแกน (ต้องกระพริบตาเพื่อยืนยันตัวตน)
* **🏎️ Threaded Camera:** ระบบจัดการกล้องแบบแยก Thread ไหลลื่น ไม่กระตุก แม้ประมวลผลหนัก
* **🧊 Smart Cooldown:** ป้องกันการบันทึกซ้ำซ้อนภายในเวลาที่กำหนด
* **💬 Telegram Notification:** ส่งรูปถ่ายพร้อมชื่อและเวลาเข้ากลุ่ม Telegram ทันทีที่สแกนติด
* **📊 Live Dashboard:** แสดงจำนวนคนมาสาย, ขาด, ลา และสถานะ Server (CPU/RAM/Disk) แบบ Real-time
* **⚙️ Admin Panel:** หน้าตั้งค่าระบบครบวงจร (เปลี่ยนกล้อง, แก้ DB, จัดการพนักงาน) โดยไม่ต้องแก้โค้ด
* **📂 Excel Export:** ดาวน์โหลดรายงานการลงเวลาเป็นไฟล์ Excel (.xlsx)

---

## 🛠️ ความต้องการระบบ (Prerequisites)

* **Python 3.9** ขึ้นไป
* **MySQL Database** (เช่น XAMPP, WAMP, หรือ MySQL Server)
* **Webcam** (USB หรือ Built-in)
* **Internet Connection** (สำหรับการส่งแจ้งเตือน Telegram และโหลดโมเดล AI ครั้งแรก)

---

## 📦 การติดตั้ง (Installation)

1.  **Clone โปรเจกต์**
    ```bash
    git clone <your-repo-url>
    cd my_attendance_project
    ```

2.  **สร้างและเปิดใช้งาน Virtual Environment (แนะนำ)**
    ```bash
    python -m venv venv
    # Windows
    venv\Scripts\activate
    # Mac/Linux
    source venv/bin/activate
    ```

3.  **ติดตั้ง Library ที่จำเป็น**
    สร้างไฟล์ `requirements.txt` แล้วใส่รายการด้านล่าง หรือรันคำสั่งติดตั้ง:
    ```bash
    pip install fastapi uvicorn opencv-python deepface tf-keras numpy mysql-connector-python pandas openpyxl jinja2 python-multipart requests psutil
    ```

4.  **ตั้งค่าฐานข้อมูล (MySQL)**
    เปิด MySQL Client (เช่น phpMyAdmin หรือ MySQL Workbench) แล้วรัน SQL Script นี้:

    ```sql
    CREATE DATABASE IF NOT EXISTS attendance_system;
    USE attendance_system;

    CREATE TABLE IF NOT EXISTS employees (
        id INT AUTO_INCREMENT PRIMARY KEY,
        name VARCHAR(100) NOT NULL,
        image_path VARCHAR(255) NOT NULL
    );

    CREATE TABLE IF NOT EXISTS attendance_logs (
        id INT AUTO_INCREMENT PRIMARY KEY,
        employee_name VARCHAR(100),
        check_time DATETIME,
        evidence_image VARCHAR(255)
    );
    ```

---

## 🚀 การใช้งาน (Usage)

1.  **รันโปรแกรม**
    ```bash
    python app.py
    ```

2.  **เข้าสู่ระบบผ่านเว็บเบราว์เซอร์**
    * เปิด: `http://localhost:9876`

3.  **ตั้งค่าระบบครั้งแรก (Admin Setup)**
    * ไปที่เมนู **⚙️ Admin** (มุมขวาบน)
    * กรอกข้อมูล **Database Connection** (Host, User, Password)
    * ตั้งค่า **Telegram Token** (ถ้าต้องการ)
    * กด **บันทึก (Save)**
    * *แนะนำ:* หากตั้งค่า Database ผิด ให้ Restart โปรแกรม 1 ครั้งหลังจากแก้ไข

---

## 📂 โครงสร้างโปรเจกต์ (Structure)

```text
my_attendance_project/
│
├── app.py                 # ไฟล์หลัก (Backend API + Logic)
├── config.json            # ไฟล์ตั้งค่าระบบ (สร้างอัตโนมัติเมื่อรัน)
├── requirements.txt       # รายชื่อ Library
│
├── templates/             # หน้าเว็บ HTML
│   ├── index.html         # หน้าสแกนหลัก
│   └── admin.html         # หน้าตั้งค่า Admin
│
├── images/                # เก็บรูปต้นฉบับสำหรับลงทะเบียน (ห้ามลบ)
└── attendance_images/     # เก็บรูปหลักฐานการสแกนเข้างาน

```

---

## 💡 คำแนะนำการใช้งานฟีเจอร์ต่างๆ

### 1. ระบบ Liveness (กระพริบตา)

* เปิดได้ที่หน้า **Admin** > หัวข้อ **System Settings** > ติ๊กถูก **👁️ Liveness**
* **การใช้งาน:** ผู้ใช้ต้องมองกล้องจนขึ้นข้อความสีแดงว่า **"Please Blink Eye"** จากนั้นให้กระพริบตา 1 ครั้ง ระบบจะเปลี่ยนเป็น **"Liveness: OK"** และทำการสแกนหน้า

### 2. การลงทะเบียนพนักงานใหม่

* ที่หน้าแรก (`/`) ใส่ชื่อในช่อง "ชื่อ-นามสกุล"
* กดปุ่ม **📸 ถ่ายภาพลงทะเบียน**
* ระบบจะบันทึกหน้าและจำใบหน้าทันที (ไม่ต้อง Retrain Model)

### 3. การใช้งานหลายเครื่อง (Multi-Station)

* ติดตั้งโปรแกรมลงในคอมพิวเตอร์หลายเครื่อง
* ที่หน้า Admin ของทุกเครื่อง ให้ตั้งค่า **Database Host** ไปที่ IP ของเครื่อง Server กลาง (เช่น `192.168.1.50`)
* แชร์โฟลเดอร์ `images` ผ่าน Network (SMB/Lan) เพื่อให้ทุกเครื่องเข้าถึงรูปต้นฉบับเดียวกัน

---

## ⚠️ ปัญหาที่พบบ่อย (Troubleshooting)

* **Q: เปิดกล้องไม่ได้ / จอดำ**
* A: ลองเปลี่ยน **Camera Index** ในหน้า Admin เป็น 0 หรือ 1 แล้วกดบันทึกและรีเฟรชหน้าเว็บ


* **Q: สแกนหน้าไม่ติด / กรอบสี่เหลี่ยมไม่ตรงหน้า**
* A: ตรวจสอบแสงสว่างหน้ากล้อง และเช็คการตั้งค่าความละเอียดกล้อง (Width/Height) ในหน้า Admin ให้ตรงกับสเปคกล้อง


* **Q: แจ้งเตือน Telegram ไม่เด้ง**
* A: ตรวจสอบ **Bot Token** และ **Chat ID** ว่าถูกต้อง และอย่าลืมดึง Bot เข้ากลุ่มไลน์/Telegram นั้นๆ



---

## 📜 License

Project นี้จัดทำขึ้นเพื่อการศึกษาและการใช้งานภายในองค์กร (Open Source)
