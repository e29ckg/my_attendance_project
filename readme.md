# 📷 HR Face Scan (Time Attendance System)

ระบบบันทึกเวลาทำงานด้วยใบหน้า (Face Recognition) ผ่าน Web Browser (Kiosk Mode) ขับเคลื่อนด้วย AI (DeepFace) พร้อมระบบหลังบ้านสำหรับจัดการพนักงาน พิมพ์รายงาน และแจ้งเตือนผ่าน Telegram

## ✨ ฟีเจอร์หลัก (Features)

* 🌐 **Web Scanner (Kiosk Mode):** สแกนใบหน้าผ่านหน้าเว็บได้ทันที ไม่ต้องลงโปรแกรมที่เครื่องลูก (รองรับ PC, Tablet, Mobile)
* 🧠 **AI Face Recognition:** ใช้โมเดล `Facenet512` ความแม่นยำสูง
* 🔐 **Secure Admin Panel:** ระบบจัดการพนักงานและรายงาน ล็อคด้วยรหัสผ่าน (HTTP Basic Auth)
* 🏢 **Role & Department Management:** จัดการประเภทพนักงาน (เช่น วิศวกร, แม่บ้าน) และตำแหน่งงานได้อย่างอิสระ
* 📸 **Evidence & Timestamp:** บันทึกรูปถ่ายพร้อมฝังเวลาลงในรูปภาพเป็นหลักฐาน
* 💬 **Telegram Notification:** แจ้งเตือนเวลาเข้า-ออกงาน พร้อมรูปถ่ายเข้ากลุ่ม Telegram แบบ Real-time
* ⚙️ **Production Ready:** รองรับการทำงานบน Ubuntu Server + Nginx + SSL (HTTPS)

---

## 📂 โครงสร้างโปรเจกต์ (File Structure)

```text
/var/www/facescan/
 ├── .env                     # ไฟล์ตั้งค่าระบบ (ต้องสร้างเอง)
 ├── requirements.txt         # รายชื่อ Library ที่ต้องใช้
 ├── server_api.py            # โค้ด Backend (FastAPI)
 │
 ├── webscan.html             # หน้าหลัก: ระบบสแกนใบหน้าผ่านเว็บ
 ├── admin.html               # ระบบจัดการพนักงาน/ตำแหน่ง
 ├── report_daily.html        # หน้ารายงานประจำวัน
 ├── report_print.html        # หน้าสำหรับพิมพ์แบบฟอร์ม (A4)
 ├── monitor.html             # หน้าตรวจสอบสถานะ Server
 │
 ├── images/                  # โฟลเดอร์เก็บรูปพนักงานต้นฉบับ
 └── attendance_images/       # โฟลเดอร์เก็บรูปลงเวลา (ลบอัตโนมัติเมื่อหมดอายุ)

```

---

## 🛠️ การตั้งค่าระบบ (Configuration)

สร้างไฟล์ชื่อ `.env` ไว้ในโฟลเดอร์เดียวกับโปรเจกต์ และกำหนดค่าดังนี้:

```env
# ==============================
# 🔧 SERVER CONFIG
# ==============================
# หากรัน Local ใช้ 0.0.0.0 | หากรันบน Ubuntu/Nginx ใช้ 127.0.0.1
HOST=127.0.0.1
PORT=9876

# ระบบ Login หน้า Admin (Basic Auth)
ADMIN_USER=admin
ADMIN_PASS=123456

# ==============================
# 🧠 AI & DATABASE
# ==============================
# ความแม่นยำ AI (0.20 - 0.40) ยิ่งน้อยยิ่งเข้มงวด
THRESHOLD=0.30
DB_FILE=attendance.db

# จำนวนวันที่จะเก็บรูปภาพหลักฐานไว้ (วัน)
KEEP_IMAGE_DAYS=15

# ==============================
# 💬 TELEGRAM NOTIFY
# ==============================
ENABLE_TELEGRAM=True
TELEGRAM_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=-100xxxxxxxxxx

```

---

## 🚀 1. การติดตั้งสำหรับทดสอบในเครื่อง (Local / Windows)

1. Clone โปรเจกต์ และเข้าไปในโฟลเดอร์
2. สร้าง Virtual Environment:
```bash
python -m venv venv
venv\Scripts\activate

```


3. ติดตั้ง Library:
```bash
pip install -r requirements.txt

```


*(หมายเหตุ: หากใช้ TensorFlow รุ่นใหม่ อาจต้องรัน `pip install tf-keras` เพิ่มเติม)*
4. สร้างไฟล์ `.env` (ดูตัวอย่างด้านบน)
5. รันเซิร์ฟเวอร์:
```bash
python server_api.py

```


6. เปิดเบราว์เซอร์ไปที่ `http://localhost:9876`

---

## 🌍 2. การติดตั้งใช้งานจริงบนเซิร์ฟเวอร์ (Production / Ubuntu)

สำหรับการนำไปใช้งานจริง (Deploy) จำเป็นต้องรันผ่าน Nginx และทำ SSL (HTTPS) เพื่อให้ Web Browser อนุญาตให้เปิดกล้องได้

### Step 1: อัปเดตระบบและติดตั้งเครื่องมือที่จำเป็น

```bash
sudo apt update
sudo apt install git python3-pip python3-venv nginx certbot python3-certbot-nginx -y

# ติดตั้ง Library พื้นฐานสำหรับ OpenCV (Headless) บน Ubuntu
sudo apt install libgl1-mesa-glx libglib2.0-0 -y

```

### Step 2: ดึงซอร์สโค้ดและตั้งค่า Python

```bash
# สร้างโฟลเดอร์และตั้งสิทธิ์
sudo mkdir -p /var/www/facescan
sudo chown -R $USER:$USER /var/www/facescan
cd /var/www/facescan

# โคลนโปรเจกต์จาก GitHub (แก้ URL ให้ตรงกับของคุณ)
git clone https://github.com/e29ckg/my_attendance_project.git .

# สร้างไฟล์ .env และใส่ค่า Config
nano .env

# สร้าง Venv และติดตั้งไลบรารี
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
pip install tf-keras

```

### Step 3: สร้าง Systemd Service (ให้ระบบรัน 24 ชม.)

1. สร้างไฟล์ Service:
```bash
sudo nano /etc/systemd/system/facescan.service

```


2. ใส่ค่าดังนี้:
```ini
[Unit]
Description=Face Scan API Service
After=network.target

[Service]
User=root
WorkingDirectory=/var/www/facescan
Environment="PATH=/var/www/facescan/venv/bin"
ExecStart=/var/www/facescan/venv/bin/python server_api.py
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target

```


3. เปิดใช้งาน Service:
```bash
sudo systemctl daemon-reload
sudo systemctl start facescan
sudo systemctl enable facescan

```



### Step 4: ตั้งค่า Nginx (Reverse Proxy)

1. สร้างไฟล์คอนฟิก:
```bash
sudo nano /etc/nginx/sites-available/facescan

```


2. ใส่ค่าดังนี้ (เปลี่ยน `facescan.yourdomain.com` เป็นโดเมนของคุณ):
```nginx
server {
    listen 80;
    server_name facescan.yourdomain.com;

    # อนุญาตให้อัปโหลดไฟล์รูปภาพได้สูงสุด 50MB
    client_max_body_size 50M;

    location / {
        proxy_pass http://127.0.0.1:9876;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

```


3. เปิดใช้งาน Nginx:
```bash
sudo ln -s /etc/nginx/sites-available/facescan /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx

```



### Step 5: ทำ SSL (HTTPS) ด้วย Certbot

รันคำสั่งด้านล่างเพื่อขอใบรับรอง SSL ฟรี (ตอบคำถามตามที่ระบบแจ้ง):

```bash
sudo certbot --nginx -d facescan.yourdomain.com

```

### Step 6: ตั้งค่า Timezone (ให้เวลาตรงกับประเทศไทย)

```bash
sudo timedatectl set-timezone Asia/Bangkok
sudo systemctl restart facescan

```

---

## 📌 การเข้าใช้งาน (URL Endpoints)

เมื่อติดตั้งเสร็จสมบูรณ์ สามารถเข้าใช้งานผ่านโดเมนของคุณได้ดังนี้:

* **หน้า Web Scanner (หน้าแรก):** `https://facescan.yourdomain.com/`
* **หน้าจัดการพนักงาน (Admin):** `https://facescan.yourdomain.com/admin` *(ต้องใส่รหัสผ่าน)*
* **หน้ารายงาน (Report):** `https://facescan.yourdomain.com/report` *(ต้องใส่รหัสผ่าน)*
* **หน้าพิมพ์รายงาน (Print):** `https://facescan.yourdomain.com/print` *(ต้องใส่รหัสผ่าน)*
* **ตรวจสอบระบบ (Monitor):** `https://facescan.yourdomain.com/monitor` *(ต้องใส่รหัสผ่าน)*