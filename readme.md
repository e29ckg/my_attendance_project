# 📷 AI Face Recognition Time Attendance System

**ระบบลงเวลาทำงานด้วยใบหน้า (Client-Server Architecture)**

ระบบบันทึกเวลาทำงานพนักงานอัตโนมัติด้วย AI (DeepFace) แยกการทำงานระหว่าง **เครื่องสแกน (Client)** และ **เครื่องประมวลผล (Server)** พร้อมระบบจัดการผ่านเว็บและรายงานผลรายวัน

---

## ✨ ฟีเจอร์หลัก (Features)

### 🖥️ ฝั่ง Client (เครื่องสแกน / Kiosk)

* **Real-time Face Detection:** ตรวจจับใบหน้าและส่งไปประมวลผลที่ Server
* **Smart Heartbeat:** เช็คสถานะ Server ตลอดเวลา (🟢 กรอบเขียว = พร้อม / 🔴 กรอบแดง = Server ดับ)
* **Voice Greeting:** พูดชื่อพนักงานเมื่อสแกนผ่าน (TTS ภาษาไทย) และไม่พูดซ้ำหากเป็นคนเดิม
* **Thai Date/Time:** แสดงวันและเวลาแบบไทย (พ.ศ.)
* **Performance:** ย่อขนาดภาพก่อนส่งและทำงานแบบ Multi-threading ไม่ทำให้หน้าจอกระตุก

### ⚙️ ฝั่ง Server (Brain & Database)

* **DeepFace AI:** ใช้โมเดล `Facenet512` แม่นยำสูง
* **RESTful API:** เชื่อมต่อผ่าน FastAPI รองรับ Client หลายตัวพร้อมกัน
* **Auto Sort Report:** รายงานเรียงตามเวลาเข้างาน (มาก่อนอยู่บน)
* **Telegram Notification:** ส่งรูปและเวลาเข้า Line/Telegram ทันทีที่สแกนติด

### 🌐 ฝั่ง Web Management (Admin & Report)

* **Dashboard:** รวมเมนูเข้าถึงง่าย (`/`)
* **Employee Management:** เพิ่ม/ลบ/แก้ไข ข้อมูลพนักงานและรูปภาพผ่านเว็บ (`/admin`)
* **Daily Report:** ดูเวลา เข้า-ออก (First-in / Last-out) รายวัน (`/report`)
* **Editable Remarks:** บันทึกหมายเหตุ (เช่น ลาป่วย, WF) ลงในรายงานได้ทันที
* **Search & Filter:** ค้นหาชื่อ/ตำแหน่ง ได้แบบ Real-time

---

## 🛠️ การติดตั้ง (Installation)

### 1. สิ่งที่ต้องมี (Prerequisites)

* Python 3.8 ขึ้นไป
* Webcam (สำหรับเครื่อง Client)

### 2. ติดตั้ง Library

รันคำสั่งนี้ทั้งที่เครื่อง **Server** และ **Client**:

```bash
pip install fastapi uvicorn python-multipart deepface opencv-python numpy requests PyQt6 gTTS pygame

```

---

## 🚀 วิธีเริ่มใช้งาน (Getting Started)

### ขั้นตอนที่ 1: รัน Server (เครื่องแม่)

1. เปิดไฟล์ `server_api.py`
2. ตั้งค่า **Telegram** (ถ้าต้องการใช้) ที่ส่วน Config ด้านบนไฟล์
3. รันคำสั่ง:
```bash
python server_api.py

```


*Server จะเริ่มทำงานที่ `http://0.0.0.0:9876*`

### ขั้นตอนที่ 2: รัน Client (เครื่องสแกน)

1. เปิดไฟล์ `client_kiosk.py`
2. แก้ไข `SERVER_URL` ให้ตรงกับ IP ของเครื่อง Server:
```python
SERVER_URL = "http://192.168.1.XX:9876"  # ใส่ IP เครื่อง Server

```


3. รันคำสั่ง:
```bash
python client_kiosk.py

```



### ขั้นตอนที่ 3: ใช้งานหน้าเว็บ (Admin/Report)

เปิด Browser (Chrome/Edge) แล้วเข้าผ่าน URL:

* **เมนูหลัก:** `http://localhost:9876` (หรือ IP Server)
* **จัดการพนักงาน:** `http://localhost:9876/admin`
* **ดูรายงาน:** `http://localhost:9876/report`

---

## 📂 โครงสร้างไฟล์ (File Structure)

```text
📦 Project_Folder
 ┣ 📜 server_api.py       # [Server] หัวใจหลัก ประมวลผล AI, DB, API
 ┣ 📜 client_kiosk.py     # [Client] หน้าจอ GUI, กล้อง, ส่งภาพไป Server
 ┣ 📜 index.html          # [Web] หน้าเมนูรวม
 ┣ 📜 admin.html          # [Web] หน้าจัดการพนักงาน (Add/Edit/Delete)
 ┣ 📜 report_daily.html   # [Web] หน้ารายงานประจำวัน
 ┣ 📜 haarcascade_frontalface_default.xml # ไฟล์ตรวจจับหน้าของ OpenCV
 ┣ 📂 images/             # (Auto) โฟลเดอร์เก็บรูปโปรไฟล์พนักงาน
 ┣ 📂 attendance_images/  # (Auto) โฟลเดอร์เก็บหลักฐานการสแกน
 ┣ 📂 sounds/             # (Auto) โฟลเดอร์เก็บไฟล์เสียง TTS
 ┗ 📜 attendance.db       # (Auto) ฐานข้อมูล SQLite

```

---

## 🔧 การตั้งค่าเพิ่มเติม (Configuration)

### การตั้งค่า Telegram

ในไฟล์ `server_api.py`:

```python
ENABLE_TELEGRAM = True
TELEGRAM_TOKEN = "YOUR_BOT_TOKEN"
TELEGRAM_CHAT_ID = "YOUR_CHAT_ID"

```

### การปรับความแม่นยำ AI

ในไฟล์ `server_api.py` ปรับค่า `THRESHOLD`:

* ค่าน้อย (เช่น 0.25) = เข้มงวดมาก (อาจจำไม่ได้ถ้าแสงเปลี่ยน)
* ค่ามาก (เช่น 0.40) = ยืดหยุ่น (แต่อาจจำผิดคน)
* *แนะนำที่ `0.30` - `0.35*`

---

## ❓ การแก้ไขปัญหาเบื้องต้น (Troubleshooting)

* **Client ขึ้น Offline 🔴:**
* ตรวจสอบว่าเครื่อง Server รันอยู่หรือไม่
* ตรวจสอบ `SERVER_URL` ใน `client_kiosk.py` ว่าใส่ IP ถูกต้องหรือไม่
* ตรวจสอบว่าเครื่อง Client และ Server อยู่ในวง LAN เดียวกัน (Ping เจอไหม)


* **สแกนแล้วไม่บันทึก/ไม่พูด:**
* ระบบมี Cooldown 1 นาที ป้องกันการบันทึกซ้ำ ให้รอก่อนแล้วสแกนใหม่
* ตรวจสอบว่าหน้าเว็บ Admin มีข้อมูลพนักงานคนนั้นและมีรูปภาพหรือไม่


* **บันทึกหมายเหตุไม่ได้:**
* ตรวจสอบว่าไฟล์ `attendance.db` มีตาราง `daily_remarks` หรือไม่ (ถ้าไม่มีให้ลบไฟล์ db ทิ้งแล้วรัน Server ใหม่เพื่อให้สร้างตาราง)



---

Enjoy your Smart Attendance System! 🎉