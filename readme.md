# 📸 AI Face Recognition Attendance System

ระบบบันทึกเวลาเข้างานด้วยใบหน้า (Face Recognition) พร้อมระบบจัดการพนักงานและ Dashboard ผ่านเว็บ
พัฒนาด้วย **Python**, **DeepFace**, **PyQt6** (สำหรับ Scanner) และ **FastAPI** (สำหรับ Web Server)

---

## ✨ ฟีเจอร์หลัก (Features)

### 🖥️ ฝั่งเครื่องสแกน (Scanner Station)
- **Face Recognition:** ตรวจจับและจดจำใบหน้าด้วยโมเดล AI (Facenet512) ที่แม่นยำ
- **Real-time Detection:** สแกนและแสดงผลชื่อภาษาไทยบนหน้าจอทันที
- **Liveness/Snapshot:** ลงทะเบียนพนักงานใหม่ได้ทันทีผ่านหน้ากล้อง
- **Notifications:** ส่งแจ้งเตือนพร้อม **รูปถ่ายหลักฐาน** เข้า Telegram ทันทีที่สแกนติด
- **Sound Feedback:** มีเสียง "ติ๊ด" ยืนยันเมื่อบันทึกสำเร็จ
- **Hardware Config:** เลือกกล้องและปรับตั้งค่า AI Threshold ได้จากหน้าจอ

### 🌐 ฝั่งเว็บจัดการ (Web Dashboard)
- **Dashboard:** ดูสรุปยอดคนมาทำงาน, ขาดงาน และกราฟสถิติรายวัน
- **User Management:** เพิ่ม, ลบ, แก้ไขข้อมูล และ **อัปโหลด/เปลี่ยนรูปโปรไฟล์พนักงาน**
- **Shift Management:** ตั้งค่ากะงาน (เช้า, บ่าย, ดึก) และมอบหมายเวรให้พนักงาน
- **Reports:** ดูประวัติการเข้างานย้อนหลัง (ผ่าน Database)

---

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)
- **Language:** Python 3.10+
- **AI/CV:** OpenCV, DeepFace, TensorFlow (Keras)
- **GUI:** PyQt6 (Desktop App)
- **Backend:** FastAPI, Uvicorn
- **Database:** MySQL
- **Frontend:** HTML5, Bootstrap 5, Jinja2 Templates

---

## ⚙️ การติดตั้ง (Installation)

### 1. Clone หรือดาวน์โหลดโปรเจกต์
```bash
git clone [https://github.com/yourusername/my_attendance_project.git](https://github.com/yourusername/my_attendance_project.git)
cd my_attendance_project

```

### 2. สร้าง Virtual Environment และติดตั้ง Library

```bash
python -m venv venv
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

# ติดตั้ง dependencies
pip install opencv-python deepface pyqt6 fastapi uvicorn mysql-connector-python pandas requests jinja2 python-multipart tf-keras pillow winsound
pip install -r requirements.txt
```

*(หมายเหตุ: `winsound` มีมาให้แล้วใน Windows หากใช้ OS อื่นต้องตัดส่วนนี้ออก)*

### 3. ตั้งค่าฐานข้อมูล (Database Setup)

เปิดโปรแกรมจัดการ MySQL (เช่น phpMyAdmin, Workbench) และรันคำสั่ง SQL ต่อไปนี้:

```sql
CREATE DATABASE IF NOT EXISTS attendance_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE attendance_system;

-- ตารางพนักงาน
CREATE TABLE employees (
    employee_id VARCHAR(50) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    role VARCHAR(50),
    image_path VARCHAR(255),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- ตารางบันทึกเวลา
CREATE TABLE attendance_logs (
    id INT AUTO_INCREMENT PRIMARY KEY,
    employee_id VARCHAR(50),
    employee_name VARCHAR(100),
    check_time DATETIME,
    evidence_image VARCHAR(255),
    status VARCHAR(20) DEFAULT 'P',
    FOREIGN KEY (employee_id) REFERENCES employees(employee_id) ON DELETE CASCADE
);

-- ตารางกะงาน
CREATE TABLE work_rules (
    role_name VARCHAR(50) PRIMARY KEY,
    start_time TIME,
    late_threshold TIME,
    end_time TIME
);

-- ตารางตารางเวร
CREATE TABLE employee_schedules (
    id INT AUTO_INCREMENT PRIMARY KEY,
    employee_id VARCHAR(50),
    role_name VARCHAR(50),
    start_date DATE,
    end_date DATE,
    FOREIGN KEY (employee_id) REFERENCES employees(employee_id) ON DELETE CASCADE
);

```

### 4. ตั้งค่า Config

สร้างไฟล์ `config.json` ที่ root folder:

```json
{
    "db_host": "localhost",
    "db_user": "root",
    "db_password": "",
    "db_name": "attendance_system",
    "camera_index": 0,
    "camera_width": 800,
    "camera_height": 600,
    "threshold": 0.35,
    "cooldown_seconds": 3600,
    "enable_cooldown": true,
    "enable_telegram": true,
    "telegram_bot_token": "YOUR_BOT_TOKEN",
    "telegram_chat_id": "YOUR_CHAT_ID"
}

```

---

## 🚀 วิธีการรันโปรแกรม (Usage)

แนะนำให้สร้างไฟล์ `.bat` หรือรันผ่าน Terminal แยกกัน 2 หน้าจอ

### 1. เปิดระบบสแกนหน้า (Desktop App)

```bash
python main_scanner.py

```

*รอประมาณ 1-2 นาทีในการเปิดครั้งแรก เพื่อโหลด AI Model*

### 2. เปิดเว็บ Dashboard (Web Server)

```bash
python main_web.py

```

*เข้าใช้งานผ่าน Browser ที่: `http://localhost:9876/manage_users*`

---

## 📂 โครงสร้างไฟล์ (File Structure)

```text
my_attendance_project/
├── main_scanner.py       # โปรแกรมสแกนหน้า (PyQt6)
├── main_web.py           # เว็บเซิร์ฟเวอร์ (FastAPI)
├── config.json           # ไฟล์ตั้งค่าระบบ
├── tahoma.ttf            # ฟอนต์ไทยสำหรับแสดงผลบนจอ
├── attendance_images/    # เก็บรูปหลักฐานการสแกน (Auto generated)
├── images/               # เก็บรูปโปรไฟล์พนักงาน (Auto generated)
└── templates/            # หน้าเว็บ HTML
    ├── dashboard.html
    └── user_management.html

```

---

## ⚠️ ปัญหาที่พบบ่อย (Troubleshooting)

1. **เปิดโปรแกรมไม่ขึ้น / ค้างหน้าดำ:**
* การรันครั้งแรก DeepFace จะดาวน์โหลดไฟล์น้ำหนักโมเดล (Weights) ขนาดใหญ่ โปรดรอสักครู่
* ตรวจสอบว่าเชื่อมต่อ MySQL สำเร็จหรือไม่


2. **กล้องไม่ติด:**
* ลองเปลี่ยนค่า `camera_index` ใน `config.json` หรือผ่านเมนูตั้งค่าในโปรแกรมเป็น 0, 1, หรือ 2
* ตรวจสอบว่ามีโปรแกรมอื่นใช้กล้องอยู่หรือไม่


3. **แจ้งเตือน TensorFlow Warning:**
* ข้อความสีแดงเกี่ยวกับ `oneDNN` หรือ `tf_keras` ใน Terminal เป็นเรื่องปกติของการเริ่มระบบ AI ไม่มีผลต่อการทำงาน



---

### 👨‍💻 Developed by

[E29CKG]

---

### 💡 สิ่งที่ผมแนะนำให้ทำต่อ:
1.  **สร้างไฟล์นี้:** สร้างไฟล์ใหม่ชื่อ `README.md` แล้ววางโค้ดข้างบนลงไป
2.  **อัปโหลดขึ้น Git:** หากคุณใช้ GitHub/GitLab ไฟล์นี้จะแสดงเป็นหน้าแรกสวยงาม ทำให้โปรเจกต์ดูเป็นมืออาชีพมากครับ
3.  **ใส่ชื่อคุณ:** แก้ไขตรง `[ชื่อของคุณ]` ด้านล่างสุดครับ

**ยินดีด้วยกับโปรเจกต์ที่เสร็จสมบูรณ์ครับ! ระบบนี้ดูดีและฟังก์ชันครบครันมากสำหรับการใช้งานจริง 🎉**
