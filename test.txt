นี่คือ **Technology Stack (สแตกการทำงาน)** ของระบบ Face Recognition Time Attendance ที่เราพัฒนากันมาครับ คุณสามารถนำข้อมูลนี้ไปใส่ใน `README.md` หรือเอกสารนำเสนอโครงการ (Proposal/Presentation) ได้เลยครับ ดูเป็นมืออาชีพและครอบคลุมทุกส่วนงานครับ

---

# 🛠️ Technology Stack (สแตกการทำงานของระบบ)

ระบบนี้ถูกออกแบบด้วยสถาปัตยกรรมแบบ **Client-Server** แยกส่วนการทำงานระหว่างเครื่องสแกนหน้าและเครื่องประมวลผล เพื่อลดภาระของเครื่องลูกข่ายและง่ายต่อการบริหารจัดการข้อมูลจากส่วนกลาง

### 1. 🏗️ Architecture (สถาปัตยกรรมระบบ)

* **Pattern:** Client-Server Architecture
* **Communication:** RESTful API (HTTP/JSON)
* **Network:** LAN / Wi-Fi (Intranet)

---

### 2. 🧠 Server Side (เครื่องแม่ข่าย & AI Core)

ส่วนหัวใจหลักของระบบ ทำหน้าที่ประมวลผลใบหน้า จัดเก็บข้อมูล และให้บริการ API

* **Language:** Python 3.10+
* **Web Framework:** **FastAPI** (เร็ว, ทันสมัย, รองรับ Asynchronous)
* **ASGI Server:** **Uvicorn** (สำหรับรัน Server ให้รองรับ High Concurrency)
* **AI Engine:** **DeepFace** (Library หลักสำหรับการจดจำใบหน้า)
* **AI Model:** **Facenet512** (โมเดลที่เลือกใช้ มีความแม่นยำสูง)
* **Image Processing:** **OpenCV** (`cv2`), **NumPy**
* **Configuration:** **python-dotenv** (จัดการตัวแปรระบบผ่านไฟล์ `.env`)

---

### 3. 🖥️ Client Side (เครื่องสแกน / Kiosk)

ส่วนหน้าจอที่พนักงานใช้งาน เน้นการตอบสนองไวและการแสดงผลที่เข้าใจง่าย

* **Language:** Python 3.10+
* **GUI Framework:** **PyQt6** (สร้างหน้าต่างโปรแกรมที่สวยงามและเสถียรบน Desktop)
* **Camera Interface:** **OpenCV** (ดึงภาพจาก Webcam)
* **HTTP Client:** **Requests** (ส่งรูปและข้อมูลไปหา Server)
* **Text-to-Speech (TTS):** **gTTS** (แปลงข้อความ Google Translate เป็นเสียงภาษาไทย)
* **Audio Playback:** **Pygame** (เล่นไฟล์เสียงทักทาย)

---

### 4. 🌐 Web Management (ส่วนจัดการ & รายงาน)

หน้าเว็บไซต์สำหรับ Admin เพื่อจัดการข้อมูลและดูรายงาน โดยเรียกใช้ API จาก Server

* **Structure:** HTML5
* **Styling:** **Bootstrap 5** (Responsive Design, สวยงามทันสมัย)
* **Icons:** **Bootstrap Icons**
* **Font:** **Sarabun** (ฟอนต์ไทยสารบัญ สำหรับงานราชการ)
* **Logic & API:** **Vanilla JavaScript** (ES6+) + **Axios** (เชื่อมต่อ API)
* **Alerts/Modals:** **SweetAlert2** (Pop-up แจ้งเตือนสวยงาม)

---

### 5. 💾 Database & Storage (ฐานข้อมูล)

* **Database Engine:** **SQLite 3** (Serverless, ไฟล์เดียวจบ, ไม่ต้องติดตั้งโปรแกรมเพิ่ม)
* **File Storage:** Local File System (เก็บรูปภาพพนักงานและหลักฐานการสแกนเป็นไฟล์ `.jpg`)

---

### 6. 🔔 Notification (การแจ้งเตือน)

* **Platform:** **Telegram Bot API**
* **Method:** Webhook / HTTP Post Request (ส่งรูปภาพและข้อความเข้ากลุ่มแชท)

---

### 📊 แผนผังการไหลของข้อมูล (Data Flow)

1. **Client (Kiosk):**
* กล้องจับภาพ ➔ ตรวจจับใบหน้า (Haar Cascade) ➔ ย่อภาพ ➔ ส่งไป Server (API `/scan`)


2. **Server (Processing):**
* รับภาพ ➔ แปลงเป็น Vector (Embedding) ด้วย DeepFace ➔ เปรียบเทียบกับฐานข้อมูล (Cosine Distance) ➔ บันทึกผลลัพธ์


3. **Notifications:**
* Server บันทึกเสร็จ ➔ ส่งแจ้งเตือนเข้า Telegram


4. **Web Admin:**
* Admin เปิดเว็บ ➔ Browser ร้องขอข้อมูล (API `/api/...`) ➔ Server ดึงจาก SQLite ➔ แสดงผลเป็นตาราง/รายงาน



---

### 💡 จุดเด่นของ Stack นี้

* **Cost-Effective:** ใช้ Open Source ทั้งหมด ไม่มีค่า License
* **Performance:** FastAPI และ Facenet512 ให้ความเร็วและความแม่นยำสูง
* **Scalability:** สามารถเพิ่มเครื่อง Client ได้หลายเครื่อง โดยใช้ Server ตัวเดิม
* **User Friendly:** หน้าเว็บ Admin ใช้งานง่าย รองรับการพิมพ์รายงานแบบฟอร์มราชการทันที